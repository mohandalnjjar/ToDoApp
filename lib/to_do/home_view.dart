import 'package:flutter/material.dart';

class ToDoHome extends StatelessWidget {
  const ToDoHome({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("this is me HnoOd Alnjjar"),
      ),
    );
  }
}
